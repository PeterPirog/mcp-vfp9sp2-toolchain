# VFP Integration Toolchain for OpenCode
# Strict read-only VFP 9 + FoxBin2Prg integration

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/

# OpenCode cache
.vfp-ai/

# IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr

# OS
.DS_Store
Thumbs.db

# Internal RAG (operational state, not for distribution)
INTERNAL_RAG/
AGENTS.md

# Agent / OpenCode local config (never for distribution)
.agents/
.opencode/

# Pre-existing tool junk (not part of toolchain)
*.db2
*.err

# Test artifacts
*.fxp

# Working scratch / local-only notes (not for distribution)
Prompt build VFP 9.0  Translator.txt

# Local audit outputs (never commit real project audits)
audit_report/

# Offline bundle build output (maintainer artifact, never committed — Phase 18)
dist/

# Sensitive anonymization artifacts (DBF_Anonymizer) — NEVER commit
dictionary.sqlite3
*_dict/
*_dictionary/
*_recovered/
*.sqlite3

# Local MCP Light Memory / OpenCode config (user-specific paths — never distribute)
opencode.json

